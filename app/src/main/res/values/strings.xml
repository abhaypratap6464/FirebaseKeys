<resources>
    <string name="app_name">FirebaseKeys</string>

    <!--
        All Firebase / Google API secrets have been moved to the NDK layer (keys.cpp).
        They are XOR-obfuscated in libkeys.so and accessed only through NativeKeys.kt.
        The Maps API key is injected into the manifest at build time via local.properties
        (git-ignored) — it no longer lives here as a plaintext resource.
    -->
</resources>
